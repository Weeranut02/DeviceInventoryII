# เขียน .gitignore (ถ้ายังไม่มี/อยากทับ)
@"
bin/
obj/
**/bin/
**/obj/
**/publish/
.vs/
.vscode/
"@ | Set-Content -Encoding ascii .gitignore

# เลิก track ไฟล์ทั้งหมด แล้ว add ใหม่ตาม .gitignore
git rm -r --cached .
git add .
git commit -m "chore: ignore build outputs (bin/ obj/ publish/)"
